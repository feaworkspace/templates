version: 1
title: Install pnpm
args:
  version: "" # Latest version by default
script: |
  export PNPM_VERSION="{{ args.version }}"
  wget -qO- https://get.pnpm.io/install.sh | sh -
  source ~/.bashrc
